import factories.interfaces.EmpleadoDao;
import models.Empleado;
import org.junit.Assert;
import org.junit.Test;

import java.util.List;

public class EmpleadoTest {
    @Test
    public void ReadTest(){
//        EmpleadoDao empleadoDao = new EmpleadoDao();
//        List<Empleado> empleados = empleadoDao.read(String.format("WHERE id = %s", 1));
//        Assert.assertNotNull(empleados);
//        for (Empleado empleado : empleados){
//            System.out.println(empleado);
//            Assert.assertNotNull(empleado);
//        }
        //Assert.assertEquals(3, regiones.size());
    }
}
